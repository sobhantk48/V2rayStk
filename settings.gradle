pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "V2rayStk"

include ":app"
project(":app").projectDir = file("android/app")

include ":core:common", ":core:ui", ":core:designsystem", ":core:model"
include ":core:database", ":core:network", ":core:datastore", ":core:domain"
include ":core:singbox"

include ":feature:profiles", ":feature:subscriptions", ":feature:groups"
include ":feature:connection", ":feature:stats", ":feature:settings"
include ":feature:admin", ":feature:scanner", ":feature:logs"
